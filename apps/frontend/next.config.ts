import type { NextConfig } from "next";

const nextConfig = {
	images: {
		domains: [
			"source.unsplash.com",
			"images.unsplash.com",
			"plus.unsplash.com",
		],
	},
};

export default nextConfig;
