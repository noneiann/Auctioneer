import React from 'react';

export default function Breadcrumb() {
  return (
    <nav className="text-sm text-main mb-4">
      Home / <span className="text-foreground font-semibold">Auctions</span>
    </nav>
  );
}
